apiVersion: apps/v1
kind: Deployment
metadata:
  name: summit-app-dep
spec:
  replicas: 2
  selector:
    matchLabels:
      app: summit-app
  template:
    metadata:
      labels:
        app: summit-app
    spec:
      containers: 
      - name: summit-app-pod
        image: totipuacr.azurecr.io/summit-app:1.0
        env:
        - name: AZSVC
          value: "Azure Kubernetes Service"
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
---
apiVersion: v1
kind: Service
metadata:
  name: summit-app-svc
spec:
  type: ClusterIP
  ports:
  - port: 80
  selector:
    app: summit-app
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: summit-app-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: azure.tipuric.com 
    http:
      paths:
      - backend:
          serviceName: summit-app-svc
          servicePort: 80
        path: /
